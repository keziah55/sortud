# sortud

Command line tool to show the size of files and directories.

See `sortud --help` for usage.

## Installation

Build with

```
cargo build --release
```

The executable resulting is `target/release/sortud`.